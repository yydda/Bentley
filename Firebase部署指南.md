# Firebase 部署指南

## 📋 前置准备

1. **Google账号**：需要一个Google账号用于登录Firebase
2. **Firebase项目**：创建一个新的Firebase项目

---

## 🚀 第一步：创建Firebase项目

### 1.1 访问Firebase控制台
1. 打开 [Firebase Console](https://console.firebase.google.com/)
2. 点击"添加项目"或"创建项目"

### 1.2 创建项目
1. **项目名称**：输入项目名称（如：`my-diary-app`）
2. **Google Analytics**：可以选择启用或禁用（免费版不需要）
3. 点击"创建项目"
4. 等待项目创建完成（约30秒）

---

## 🔧 第二步：配置Firebase Web应用

### 2.1 添加Web应用
1. 在Firebase控制台中，点击项目设置图标（⚙️）
2. 向下滚动到"你的应用"部分
3. 点击"</>"（Web应用图标）
4. **应用昵称**：输入应用名称（如：`Diary App`）
5. **勾选**：Firebase Hosting（可选，我们使用GitHub Pages）
6. 点击"注册应用"

### 2.2 获取配置信息
1. 复制显示的配置对象，类似：
```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
}
```

### 2.3 更新项目配置
1. 打开 `src/config/firebase.js`
2. 将 `YOUR_API_KEY` 等占位符替换为实际的配置值

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",  // 替换这里
  authDomain: "your-project.firebaseapp.com",  // 替换这里
  projectId: "your-project-id",  // 替换这里
  storageBucket: "your-project.appspot.com",  // 替换这里
  messagingSenderId: "123456789",  // 替换这里
  appId: "1:123456789:web:abcdef"  // 替换这里
}
```

---

## 🔐 第三步：启用Google认证

### 3.1 启用Google登录
1. 在Firebase控制台，点击左侧菜单的"Authentication"
2. 点击"开始使用"
3. 点击"Sign-in method"标签
4. 找到"Google"，点击启用
5. **项目支持邮箱**：选择你的Google账号邮箱
6. **项目名称**：输入项目名称（会自动填充）
7. 点击"保存"

### 3.2 配置授权域名（重要）

**方法一：通过Authentication设置**
1. 在Firebase控制台，点击左侧菜单的 **"Authentication"**
2. 在页面顶部，点击 **"Settings"（设置）** 标签（在"Sign-in method"标签旁边）
3. 向下滚动找到 **"Authorized domains"（授权域名）** 部分
4. 点击 **"Add domain"（添加域名）** 按钮
5. 输入你的GitHub Pages域名（**不需要** `https://` 前缀），例如：
   - `yourusername.github.io`
6. 点击"Add"保存

**方法二：通过项目设置**
1. 在Firebase控制台，点击左上角的 **设置图标（⚙️）**
2. 选择 **"Project settings"（项目设置）**
3. 向下滚动找到 **"Authorized domains"（授权域名）** 部分
4. 点击 **"Add domain"** 添加你的GitHub Pages域名

**重要提示**：
- 默认已经包含 `localhost`、`your-project.firebaseapp.com` 和 `your-project.web.app`
- 只需要添加你的GitHub Pages根域名即可（例如：`yourusername.github.io`）
- **不需要**包含路径，例如不要添加 `yourusername.github.io/repo-name`
- 如果找不到这些选项，可以尝试直接访问：`https://console.firebase.google.com/project/bentley-c62d5/authentication/settings`

**详细说明**：如果还是找不到，请查看 `Firebase授权域名配置说明.md` 文件，里面有更详细的步骤和截图说明。

---

## 🗄️ 第四步：配置Firestore数据库

### 4.1 创建Firestore数据库

**如果左侧菜单中没有"Firestore Database"：**

1. 在Firebase控制台，点击左侧菜单的 **"Build"（构建）** 或 **"构建"** 选项
2. 在展开的菜单中，找到并点击 **"Firestore Database"** 或 **"Firestore 数据库"**
3. 如果还是没有，尝试点击左侧菜单的 **"更多"** 或 **"More"** 选项

**如果仍然找不到，按以下步骤创建：**

1. 在Firebase控制台首页，找到 **"Firestore Database"** 卡片
2. 点击 **"创建数据库"** 或 **"Create database"** 按钮
3. 如果首页也没有，尝试直接访问：
   ```
   https://console.firebase.google.com/project/bentley-c62d5/firestore
   ```

**创建数据库步骤：**

1. 点击 **"创建数据库"** 或 **"Create database"**
2. **安全规则**：选择 **"以测试模式启动"** 或 **"Start in test mode"**（开发阶段）
   - ⚠️ **注意**：测试模式允许所有读写，稍后需要配置安全规则
3. **位置**：选择离你最近的区域
   - 推荐选择：`asia-east1`（台湾）或 `asia-southeast1`（新加坡）
   - 如果在中国大陆，可以选择：`asia-east1`
4. 点击 **"启用"** 或 **"Enable"**
5. 等待数据库创建完成（约30-60秒）

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if
          request.time < timestamp.date(2026, 1, 29);
    }
  }
}

**如果已经创建过数据库：**
- 直接点击左侧菜单的 **"Firestore Database"** 即可进入
- 或者访问上面的直接链接

### 4.2 配置安全规则（重要）
1. 在Firestore Database页面，点击"规则"标签
2. 替换为以下规则：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 日记数据：只有登录用户才能读写自己的数据
    match /diaries/{userId}/dates/{date} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 人生主线数据：只有登录用户才能读写自己的数据
    match /lifeThreads/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

3. 点击"发布"

**规则说明**：
- 只有登录用户才能访问
- 用户只能读写自己的数据（通过 `userId` 匹配）
- 确保数据安全隔离

---

## 📦 第五步：安装依赖

在项目根目录运行：

```bash
npm install
```

这会安装Firebase SDK（已在 `package.json` 中添加）

---

## 🧪 第六步：本地测试

### 6.1 启动开发服务器
```bash
npm run dev
```

### 6.2 测试功能
1. 打开浏览器访问 `http://localhost:5173`
2. 点击"登录"按钮
3. 使用Google账号登录
4. 填写日记数据
5. 检查数据是否保存到Firebase

### 6.3 检查Firebase控制台
1. 打开Firestore Database
2. 应该能看到 `diaries` 集合
3. 展开后能看到你的用户ID和日期数据

---

## 🚀 第七步：部署到GitHub Pages

### 7.1 创建GitHub仓库

#### 7.1.1 在GitHub上创建新仓库
1. 访问 [GitHub](https://github.com/)
2. 点击右上角的 **"+"** 按钮，选择 **"New repository"**
3. 填写仓库信息：
   - **Repository name**：输入仓库名称（如：`my-diary-app`）
   - **Description**：可选，填写描述
   - **Visibility**：
     - **如果使用GitHub Pages（免费）**：必须选择 **Public**（公开）
     - **如果使用Firebase Hosting或其他部署方式**：可以选择 **Private**（私有）
     - **如果使用GitHub Pro/Team账户**：可以选择 **Private**（私有）
   - **不要**勾选 "Initialize this repository with a README"（如果本地已有代码）
4. 点击 **"Create repository"**

> **💡 关于仓库可见性说明：**
> - **GitHub Pages免费版**：只支持公开仓库，代码会公开可见
> - **GitHub Pages付费版**（Pro/Team）：支持私有仓库
> - **Firebase Hosting**：支持私有仓库，代码不会公开
> - **数据安全性**：即使仓库公开，Firebase数据仍然安全（通过Firebase安全规则保护）

#### 7.1.2 初始化本地Git仓库（如果还没有）
```bash
# 在项目根目录执行
git init
```

#### 7.1.3 连接本地仓库到GitHub
```bash
# 添加远程仓库（将 YOUR_USERNAME 和 YOUR_REPO_NAME 替换为你的实际值）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 或者使用SSH（如果你配置了SSH密钥）
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git
```

### 7.2 构建项目
```bash
npm run build
```

### 7.3 提交代码
```bash
# 添加所有文件
git add .

# 提交代码
git commit -m "添加Firebase支持"

# 推送到GitHub（首次推送）
git branch -M main
git push -u origin main
```

### 7.4 配置GitHub Pages

#### 7.4.1 启用GitHub Pages
1. 在GitHub上打开你的仓库
2. 点击顶部的 **"Settings"**（设置）标签
3. 在左侧菜单中找到 **"Pages"**（页面）选项
4. 在 **"Source"**（源）部分：
   - 选择 **"Deploy from a branch"**（从分支部署）
   - **Branch**：选择 `main` 分支
   - **Folder**：选择 `/ (root)` 或 `/docs`（如果构建输出在docs文件夹）
5. 点击 **"Save"**（保存）

#### 7.4.2 使用GitHub Actions自动部署（推荐）
如果项目中有 `.github/workflows/deploy.yml` 文件：
1. 在 **Settings > Pages** 中
2. 将 **Source** 改为 **"GitHub Actions"**
3. 每次推送到 `main` 分支时，会自动构建并部署

#### 7.4.3 等待部署完成
- 部署通常需要1-2分钟
- 部署完成后，会显示你的网站地址：
  - **如果使用根域名**：`https://YOUR_USERNAME.github.io/`
  - **如果使用子路径**：`https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/`

> **⚠️ 重要提示：关于子路径部署**
> 
> 如果你的 `YOUR_USERNAME.github.io` 已经用于其他项目（如博客），新项目需要部署到子路径：
> 
> 1. **仓库名就是子路径**：例如仓库名是 `bentley`，访问地址就是 `yydda.github.io/bentley/`
> 2. **配置已自动处理**：项目已配置为自动使用仓库名作为子路径
> 3. **如果使用根域名**：在 `.github/workflows/deploy.yml` 中注释掉 `VITE_BASE_PATH` 环境变量

### 7.5 更新授权域名
1. 在Firebase控制台的Authentication设置中
2. 添加你的GitHub Pages域名到授权域名列表
3. 域名格式：
   - **根域名**：`YOUR_USERNAME.github.io`（不需要包含仓库路径）
   - **子路径部署**：同样只需要添加 `YOUR_USERNAME.github.io`（Firebase会自动处理子路径）

---

## 🔒 安全注意事项

### ⚠️ API Key安全
- Firebase的API Key是**公开的**，这是正常的设计
- 安全性通过**Firestore安全规则**保证
- 不要担心API Key泄露，只要安全规则配置正确即可

### ✅ 安全最佳实践
1. ✅ 使用Firestore安全规则限制访问
2. ✅ 确保用户只能访问自己的数据
3. ✅ 定期检查Firebase控制台的使用情况
4. ✅ 监控异常访问

---

## 📊 免费额度说明

Firebase免费版（Spark计划）包含：

| 服务 | 免费额度 |
|------|---------|
| Firestore读取 | 50,000次/天 |
| Firestore写入 | 20,000次/天 |
| Firestore删除 | 20,000次/天 |
| 存储空间 | 1GB |
| 带宽 | 10GB/月 |
| Authentication | 无限制 |

**个人使用完全足够！**

---

## 🐛 常见问题

### Q1: 登录后提示"权限不足"
**A:** 检查Firestore安全规则是否正确配置，确保用户ID匹配

### Q2: 数据没有同步
**A:** 
1. 检查是否已登录
2. 检查浏览器控制台是否有错误
3. 检查Firebase控制台的网络请求

### Q3: 本地数据如何迁移到Firebase？
**A:** 
1. 登录后，在登录对话框中会显示"检测到本地数据"
2. 点击"立即迁移"按钮
3. 数据会自动迁移到Firebase

### Q4: 可以同时使用本地存储和Firebase吗？
**A:** 可以！系统支持两种模式：
- **云端同步模式**：数据存储在Firebase，多设备同步
- **本地存储模式**：数据存储在浏览器，不同步

可以在登录对话框中切换模式。

---

## 📝 下一步

1. ✅ 完成Firebase配置
2. ✅ 测试登录和数据同步
3. ✅ 部署到GitHub Pages
4. ✅ 在不同设备上测试同步功能

---

## 🆘 需要帮助？

如果遇到问题：
1. 检查Firebase控制台的错误日志
2. 检查浏览器控制台的错误信息
3. 查看Firebase官方文档

**祝你使用愉快！** 🎉

